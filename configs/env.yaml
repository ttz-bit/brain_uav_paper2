project:
  name: "brain_uav_paper2"
  stage: "interface_freeze"

world:
  unit_name: "abstract_world_unit"
  dt: null
  x_range: null
  y_range: null

aircraft:
  state_dim: 4
  speed: null
  heading_dim: 1

target:
  state_dim: 4
  default_mode: "cv"
  motion_modes:
    - "cv"
    - "turn"
    - "piecewise"
    - "evasive"

no_fly_zone:
  max_count: null
  radius_min: null
  radius_max: null

observation:
  use_target_truth: true
  use_vision_observation: true
  use_target_estimate: true